# Archived JSON knowledge layer (2026-09-02)

Snapshot of the pre-vault JSON knowledge files. Kept so the migration is reversible and so `scripts/build_knowledge.py` can still rebuild this layer from `data/raw/pages`.

The live knowledge product is the Obsidian vault at `knowledge/`.
