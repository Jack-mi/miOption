# 跑通

```bash
python -m signal_chain.orchestrator --tickers US.AAPL --no-llm
```

没有模型时，研究和价值弃权，简报仍然写出走势、合成和结构菜单。有 `DEEPSEEK_API_KEY` 时去掉 `--no-llm`。富途 OpenD 在 `127.0.0.1:11111` 时，报价和期权链走富途；没有则报价可以落到 Yahoo。
